<p id="notice"><%= notice %></p>
<h1>記事</h1>
<p><%= flash[:success] %></p>
<p><%= flash[:alert] %></p>
<%= flash[:notice] %>
<%
=begin%>
 <% @article.errors %>
<%
=end%>
<%= link_to '記事作成', "/articles/new", class: "btn btn-info" %>
<table class="table">
  <thead>
    <tr>
      <th scope="col">タイトル</th>
      <th scope="col">内容</th>
    </tr>
  </thead>
    <tbody>
  <% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.content %></td>
    <td class="hstack gap-3">
      <%= link_to '詳細', "/articles/#{article.id}", class: "btn btn-success mr-1" %>
      <%= link_to '編集', "/articles/#{article.id}/edit", class: "btn btn-warning" %>
      <%= button_to '削除', article , class: "btn btn-danger", method: :delete %>
    </td>
  </tr>
  <% end %>
  </tbody>
</table>
