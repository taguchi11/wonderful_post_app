<div class="card">
  <div class="card-header">
    <%= @article.title %>
  </div>
  <div class="card-body">
    <%= @article.content %>
  </div>
</div>
<%= flash[:notice] %>
<%= link_to '編集', "/articles/#{@article.id}/edit"  class: "btn btn-warning" %>
<%= link_to '一覧', "/" class: "btn btn-secondary" %>
